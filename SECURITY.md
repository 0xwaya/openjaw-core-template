# Security Notes

- Never commit real secrets (`.env`, API keys, tokens, private keys).
- Keep runtime credentials in environment variables or secret managers.
- Review generated files before running in production contexts.
- Enforce branch protection and PR review for shared repos.
